"### 🏔️ Exercise 2: The End-of-Season Price Adjuster!\n\nLet's build a dynamic price reduction endpoint completely from scratch!\n\n#### 🎯 Your Mission:\n\nWrite a new server route and controller function from scratch to handle:\n\n\\* \\*\\*POST /api/tours/apply-discount\\*\\*\n \\* \\*\\*SQL Query Goal:\\*\\*\n \\* Execute an `UPDATE tours` query using `CASE` to adjust `price`:\n \\* When `difficulty = 'Hard'`", " set `price = price * 0.80`.\n \\* When `difficulty = 'Medium'`", " set `price = price * 0.90`.\n \\* Else keep `price` the same.\n \\* \\*\\*JavaScript Goal:\\*\\*\n \\* Execute using \\*\\*db.run()\\*\\* (since this modifies data).\n \\* Handle errors with `return;` inside your `if (err)` check.\n \\* Return a `200 OK` JSON response: `{ message: \"Seasonal discounts applied to all tours successfully!\" }`.\n\nGive this dynamic `UPDATE` endpoint a shot completely from scratch! Paste your code below when you are ready. 🦾"

[
